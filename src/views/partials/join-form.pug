div.form-wrapper
    if errorMessage
        div.errorMessage-box
            span.errorMessage=errorMessage
    h1 환영합니다
    p 필수 정보만 입력하고 시작해보세요!
    form(method="POST")
        div.input-box
            label(for="email") *이메일: 
            input(type="email", name="email", required)#email
            input(value=false, name="emailValidation").hidden#emailValidation
            button.validation-check-btn#email-val-btn 중복 검사
        div.info-box
            p#emailValResult
        div.input-box 
            label(for="name") *닉네임: 
            input(type="text", name="name", required)#name
        div.input-box
            label(for="password") *비밀번호: 
            input(type="password", name="password", required)#password
            input(value=false, name="passwordValidation").hidden#passwordValidation
        div.input-box.pw-confirm
            label(for="password") *비밀번호 확인: 
            input(type="password", name="passwordConfirm", required)
        div.btn-box 
            button 가입 완료
    hr
    p.social 소셜 아이디로 계속하기
    include oauth-box.pug